pp.get('/edit/:studentId', function(req, res) {
  // 根据studentId从数据库或其他数据源获取学生信息
  var student = getStudentById(req.params.studentId);

  // 渲染模板并传递学生信息
  res.render('editStudent', { student: student });
});
