const express = require('express');
const { MongoClient } = require('mongodb');

const app = express();
const dbName = 'User';
const collectionName = 'user';
const uri = 'mongodb+srv://billydeng97:dhy97886886@cluster0.zsgzyzj.mongodb.net/?retryWrites=true&w=majority';

app.get('/', (req, res) => {
  MongoClient.connect(uri, { useNewUrlParser: true, useUnifiedTopology: true }, (err, client) => {
    if (err) {
      console.error('Failed to connect to MongoDB:', err);
      res.status(500).send('Failed to connect to MongoDB');
      return;
    }

    const db = client.db(dbName);

    db.collection(collectionName).findOne({ "username": "dhy97", "password": "200755" }, (err, result) => {
      if (err) {
        console.error('Failed to query MongoDB:', err);
        res.status(500).send('Failed to query MongoDB');
        return;
      }

      client.close();

      if (result) {
        res.send('User found');
      } else {
        res.send('User not found');
      }
    });
  });
});

app.listen(3000, () => {
  console.log('Server is running on port 3000');
});
